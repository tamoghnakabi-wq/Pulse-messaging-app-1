/**
 * Public type barrel — domain types live under `shared/types`.
 * Keep this path stable for existing imports.
 */
export type {
  User,
  UserSettings,
  SessionInfo,
  Attachment,
  Reaction,
  Message,
  MessageType,
  LinkPreview,
  SenderIdentity,
  Participant,
  Conversation,
  ConversationPrefs,
  SidebarFilter,
  CallState,
  CallType,
  CallStatus,
  CallMember,
  GroupMemberStatus,
  NotificationItem,
} from '../shared/types';
